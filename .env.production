# Production Environment Variables for Vercel
# These are minimal settings to get the app running
# You'll need to add these to Vercel dashboard for production

# Database - Use a cloud PostgreSQL service
DATABASE_URL=postgresql://user:password@host:5432/verdict360?sslmode=require

# Security - Generate secure keys for production
JWT_SECRET=your-secure-jwt-secret-min-32-chars
ENCRYPTION_KEY=your-64-char-hex-encryption-key

# Redis - Use Upstash or similar
REDIS_URL=redis://default:password@host:port

# Application
NODE_ENV=production
PUBLIC_DOMAIN_SUFFIX=verdict360.com

# Email (Optional for initial deployment)
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USER=apikey
SMTP_PASS=your-sendgrid-api-key
EMAIL_FROM=noreply@verdict360.com

# AI Service (Optional for initial deployment)
OLLAMA_BASE_URL=https://your-ai-endpoint.com