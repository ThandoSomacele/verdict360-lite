# Staging Environment for Investor Demo
# Using free tier services for demonstration

# Supabase Free Tier (Recommended for staging)
# Sign up at: https://supabase.com
# Create project and get connection string from Settings > Database
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres?sslmode=require

# Alternative: Neon Free Tier
# Sign up at: https://neon.tech
# DATABASE_URL=postgresql://[user]:[password]@[endpoint].neon.tech/[database]?sslmode=require

# Security (Generate for staging)
JWT_SECRET=staging-jwt-secret-replace-in-production-32chars
ENCRYPTION_KEY=staging-encryption-key-replace-in-production-64-char-hex-string

# Redis (Optional - Upstash free tier)
# Sign up at: https://upstash.com
REDIS_URL=redis://default:[password]@[endpoint].upstash.io:6379

# Application
NODE_ENV=staging
PUBLIC_DOMAIN_SUFFIX=verdict360-lite.vercel.app

# Email (Optional for staging - use Resend free tier)
# Sign up at: https://resend.com
EMAIL_FROM=noreply@verdict360-demo.com
RESEND_API_KEY=re_staging_key

# AI Service (Use local Ollama for demo)
OLLAMA_BASE_URL=http://localhost:11434

# Stripe (Test mode keys)
STRIPE_PUBLISHABLE_KEY=pk_test_staging
STRIPE_SECRET_KEY=sk_test_staging

# Demo Configuration
IS_STAGING=true
DEMO_MODE=true
ALLOW_DEMO_LOGIN=true