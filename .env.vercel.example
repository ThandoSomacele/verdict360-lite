# Vercel Environment Variables Example
# Copy this to Vercel dashboard environment settings

# Database (Use Vercel Postgres, Neon, or Supabase)
DATABASE_URL=postgresql://user:password@host:5432/verdict360?sslmode=require

# Redis (Use Upstash for serverless)
REDIS_URL=redis://default:password@host:port

# Security (Generate secure random strings)
JWT_SECRET=your-secure-jwt-secret-min-32-chars
ENCRYPTION_KEY=your-64-char-hex-encryption-key

# Stripe (Optional for payment testing)
STRIPE_SECRET_KEY=sk_test_...
STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Email Service (Use SendGrid, Postmark, or AWS SES)
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USER=apikey
SMTP_PASS=your-sendgrid-api-key
EMAIL_FROM=noreply@verdict360.com

# Google Calendar Integration (Optional)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=https://your-app.vercel.app/api/auth/google/callback

# AI Service (Use OpenAI API or hosted Ollama endpoint)
OLLAMA_BASE_URL=https://your-ollama-endpoint.com
# Or use OpenAI
OPENAI_API_KEY=sk-...

# Application Config
PUBLIC_DOMAIN_SUFFIX=verdict360.com
NODE_ENV=production

# Vercel-specific
VERCEL_URL=$VERCEL_URL